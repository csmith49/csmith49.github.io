<div class="navigation">
    <a class="name" href="/">{{ about.name }}</a>
    <nav class="nav-items">{%- for entry in navigation %}
        <a href={{ entry.url }}>{{ entry.name }}</a>
    {% endfor %}</nav>
</div>